numpy
scipy
# CPU-only version
#jax
# GPU version with CUDA 12
jax[cuda12]
cupy-cuda12x
# GPU version with CUDA 13 (recommended)
#jax[cuda13]
#cupy-cuda13x
# Intel MKL Pardiso solver (optional)
#pypardiso
